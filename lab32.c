#include <stdio.h>
#include <math.h>
#define g 9.8
#define pi 3.14
int main()
{
    float t;
    int l;
    l = l + 10;
    t = 2 * pi * sqrt(1 / g);
    printf("Time period of pendulum for %dm of thread is %f", l, t);
    l = l + 10;
    t = 2 * pi * sqrt(1 / g);
    printf("\nTime period of pendulum for %dm of thread is %f", l, t);
    l = l + 10;
    t = 2 * pi * sqrt(1 / g);
    printf("\nTime period of pendulum for %dm of thread is %f", l, t);
    l = l + 10;
    t = 2 * pi * sqrt(1 / g);
    printf("\nTime period of pendulum for %dm of thread is %f", l, t);
    l = l + 10;
    t = 2 * pi * sqrt(1 / g);
    printf("\nTime period of pendulum for %dm of thread is %f", l, t);

    return 0;
}   